name: Nightly Build

on:
  schedule:
    # Run every Sunday at 2 AM UTC (weekly)
    - cron: '0 2 * * 0'
  workflow_dispatch:

jobs:
  nightly-tests:
    name: Nightly Test Suite
    runs-on: ubuntu-latest
    
    strategy:
      matrix:
        python-version: ['3.11']
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v5
      with:
        python-version: ${{ matrix.python-version }}
        cache: 'pip'
    
    - name: Install system dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y libgl1 libglib2.0-0
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
    
    - name: Run full test suite
      run: |
        pytest tests/ -v --tb=short --maxfail=5
    
    - name: Run unit tests
      run: |
        pytest tests/unit -m "unit" -v --tb=short
    
    - name: Run integration tests
      run: |
        pytest tests/integration -m "integration" -v --tb=short
      continue-on-error: true
    
    - name: Run e2e tests
      run: |
        pytest tests/e2e -m "e2e" -v --tb=short
      continue-on-error: true
    
    - name: Run property-based tests
      run: |
        pytest tests/property_based -m "property_based" -v --tb=short --no-cov
      continue-on-error: true
    
    - name: Run slow tests
      run: |
        pytest tests/ -m "slow" -v --no-cov || true
      continue-on-error: true
    
    - name: Run performance regression tests (sequential only)
      run: |
        pytest tests/performance/regression -m "performance and not requires_spark" -v --tb=short --no-cov || true
      continue-on-error: true

  notebooks:
    name: Notebook Validation
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
        cache: 'pip'
    
    - name: Install notebook dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install jupyter ipywidgets nbconvert jupyterlab
    
    - name: Validate all notebooks
      run: |
        if [ -d "notebooks" ]; then
          python scripts/test_notebooks.py notebooks/ --mode validate
        else
          echo "⚠️ Notebooks directory not found - skipping validation"
        fi
      continue-on-error: true
    
    - name: Execute notebooks (check for runtime errors)
      run: |
        if [ -d "notebooks" ]; then
          echo "🚀 Executing notebooks to check for runtime errors (nightly: first 10 notebooks)..."
          # For nightly builds, test a subset (first 10) to save time
          python scripts/test_notebooks.py notebooks/ --mode execute --limit 10
        else
          echo "⚠️ Notebooks directory not found - skipping execution"
        fi
      continue-on-error: true
      env:
        NUMBA_DISABLE_JIT: 1
        CI: true
        GITHUB_ACTIONS: true

  code-quality:
    name: Code Quality Check
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
        cache: 'pip'
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install black flake8 pylint mypy bandit safety
    
    - name: Security check with bandit
      run: |
        bandit -r src/ -f json -o bandit-report.json || true
      continue-on-error: true
    
    - name: Dependency vulnerability check
      run: |
        safety check --json || true
      continue-on-error: true
    
    - name: Upload security reports
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: security-reports
        path: bandit-report.json

